# checker_async_aiohttp.py - استيراد مباشر من checker_aiohttp

from checker_aiohttp import (
    check_card,
    get_random_site,
    normalize_proxy,
    _is_dead,
    _mark_dead,
    _exc_text,
    normalize_result,
    reload_sites,
    site_count,
    dead_site_count,
    validate_proxy,
    validate_proxy_info,
)